# Breaking changes in v20

- Flat config now enforces `tseslint.configs.strictTypeChecked` instead of `tseslint.configs.recommendedTypeChecked`, which could raise additional errors in usage.
- `eslint/recommended` v9 rule changes backported to legacy config, which could raise additional errors in usage.
